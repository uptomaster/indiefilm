# 업데이트 요약

## 완료된 작업

### 1. Firestore 인덱스 문제 해결 ✅
- `getActors` 함수를 수정하여 인덱스 없이 작동하도록 변경
- 필터링은 클라이언트 사이드에서 처리하여 복합 인덱스 불필요

### 2. 네비게이션 사이드바 추가 ✅
- `web/components/Navigation.tsx` 생성
- 모든 페이지에 사이드바 추가 (레이아웃에 통합)
- 홈, 배우 목록, 영화 목록, 내 프로필, 영화 업로드 메뉴 포함
- 사용자 정보 및 로그아웃 기능 포함

### 3. 프로필 페이지 구조 변경 ✅
- `/actors/me` → 프로필 상세 보기로 리다이렉트 (`/actors/me/view`)
- `/actors/me/view` → 프로필 상세 보기 페이지 (읽기 전용)
- `/actors/me/edit` → 프로필 수정 페이지 (폼)

### 4. 이미지 업로드 기능 준비 ✅
- `web/lib/storage.ts` 생성
- Firebase Storage 업로드 함수 구현
- 이미지 업로드 UI 추가 준비 완료

## 진행 중인 작업

### 이미지 업로드 기능 완성
- 프로필 사진 업로드 UI 추가 필요
- 갤러리 이미지 업로드 UI 추가 필요

### 프로필 수정 페이지 이동
- 기존 `/actors/me/page.tsx`의 수정 폼을 `/actors/me/edit/page.tsx`로 이동 필요

## 다음 단계

1. **Firestore 인덱스 생성** (선택사항)
   - 콘솔에서 제공된 링크로 인덱스 생성 가능
   - 현재는 인덱스 없이도 작동하도록 수정됨

2. **프로필 목록 표시 문제 해결**
   - Firebase 콘솔에서 `actors` 컬렉션 확인
   - `isPublic` 필드가 `true`인지 확인
   - 프로필 저장 후 목록 페이지 새로고침

3. **이미지 업로드 기능 완성**
   - 프로필 사진 업로드 버튼 추가
   - 갤러리 이미지 업로드 버튼 추가
   - 업로드 진행 상태 표시

4. **프로필 수정 페이지 이동**
   - `/actors/me/edit/page.tsx` 파일 생성
   - 기존 수정 폼 코드 이동

## 참고사항

- 네비게이션은 레이아웃에 통합되어 모든 페이지에 자동으로 표시됩니다
- 프로필이 없으면 자동으로 수정 페이지로 리다이렉트됩니다
- 프로필 저장 후 상세 보기 페이지로 이동합니다
